{
  "name": "ecothrift-dashboard",
  "version": "2.2.6",
  "engines": {
    "node": "18.x",
    "npm": "9.x"
  },
  "scripts": {
    "heroku-postbuild": "cd frontend && npm install --include=dev && npm run build"
  }
}
