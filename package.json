{
  "name": "ecothrift-dashboard",
  "version": "2.37.0",
  "engines": {
    "node": "18.x",
    "npm": "9.x"
  },
  "scripts": {
    "heroku-postbuild": "cd frontend && npm install --include=dev && npm run build && cd ../frontend-public && npm install --include=dev && npm run build"
  }
}
