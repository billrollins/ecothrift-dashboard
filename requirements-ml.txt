# ML/Data Science dependencies for the pricing model and category classifier
# Install with: pip install -r requirements-ml.txt
#
# These are OPTIONAL — the app runs without them using heuristic fallbacks.
# Required to train and use the ML-based price estimator and category model.

scikit-learn>=1.4.0
pandas>=2.0.0
joblib>=1.3.0

# Gradient boosting models — install at least ONE for best price prediction:
lightgbm>=4.0.0      # preferred: faster, better on tabular data
# xgboost>=2.0.0    # alternative if lightgbm fails to install

# Optional: for data analysis and the audit report
numpy>=1.26.0
