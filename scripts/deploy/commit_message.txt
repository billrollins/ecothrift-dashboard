feat(retag): bulk label qty on create, staggered print, reset qty after multi-unit

- POST /api/inventory/retag/v2/create/ accepts optional quantity (1–50, default 1);
  transaction creates N Items and RetagLog rows, updates TempLegacyItem once; returns
  quantity, created[] (new_sku + print_payload per row), and legacy top-level fields
  for the last item (apps/inventory/views.py).
- RetagPage: Session "Labels / qty"; passes quantity to create; prints each label via
  existing POST /print/label with 200 ms stagger; after a successful multi-unit run,
  resets Labels / qty to 1 (frontend/src/pages/inventory/RetagPage.tsx).
- Client types: RetagV2CreateRequest.quantity, RetagV2CreateRow, RetagV2CreateResponse.created
  (frontend/src/api/inventory.api.ts).

Release: v2.2.5 bulk retag + print; v2.2.6 qty reset UX (CHANGELOG.md). Steering:
.ai/context.md, .ai/extended/inventory-pipeline.md. Outside named initiative — retag ops.
