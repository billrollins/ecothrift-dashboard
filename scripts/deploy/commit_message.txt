release(v2.42.0): floorplan bulk image tools and orphan image cleanup

Release: v2.42.0

- Ctrl+A selects every object on the plan.
- Multi-element image tools in the properties panel: set one image across the selection (library pick or Choose from file upload), clear to solid color, or Reset to kind defaults (adopts each kind's CURRENT default image, so updated defaults propagate).
- Per-element Reset to kind default; upload buttons relabeled "Choose from file..." (instance panel + kind dialog).
- Orphan image cleanup: purge_orphan_assets service hard-deletes assets unreferenced by any active plan element or kind default; 24h grace for fresh uploads; soft-deleted rows exempt. Runs after plan saves, kind writes, asset deletes.
- Asset pickers refresh after saves/kind writes (query invalidation).
- Tests: 7 new backend purge tests (apps.floorplan 51 OK); vitest 213 OK; tsc + build green.

Initiative: .ai/initiatives/floorplan_builder.md

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
