release(v2.47.0): floorplan wall cutter tool

Release: v2.47.0

- New Cut tool (toolbar scissors, key C): hover a wall to see a dashed grid-snapped cut line; click to split it into two independent segments (cutWallAt - rotation-aware split along the wall's length; both pieces keep kind, thickness, rotation, flip, group; both selected after; tool stays active for repeated cuts).
- Only wall-kind elements respond; locked walls and cuts that would leave a stub are ignored.
- Fix: removed stray UTF-8 BOM from root package.json (broke local vitest resolution; npm/Heroku unaffected).
- Tests: 3 new cutWallAt cases (vitest 229 OK); tsc + production build green.

Initiative: .ai/initiatives/floorplan_builder.md

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
