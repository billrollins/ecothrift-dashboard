feat(inventory): preprocessing three-layer, ai_status Final Review, llm_chat + Grok

Ships backlog on main since v2.20.0 tag: PO/manifest steering, preprocessing redesign,
cleanup CSV/Grok wiring, core LLM abstraction, docs/protocols — CHANGELOG stays [Unreleased];
no semver bump per review.0.Bump Part 2A.

Inventory / preprocessing
- Three-layer PreprocessingRow (standard_*, ai_*, final_*); migrations 0036/0038; finalize
  coalesce + ManifestRow rebuild; lean download-cleanup-csv (base_cost, ideal_price);
  apply/upload-cleanup-csv wide vs narrow + optional ai_status + relaxed wide validation.
- Final Review UI: ai_status chips; PATCH clears ai_status on listing/price edits;
  cleanupCsv thirteen-column parsing + soft_warnings surfaced in Step 2.
- suggest-formulas uses llm_chat_completion_text; XAI_API_KEY/GROK_API_KEY, AI_PROVIDER,
  openai SDK dependency.

Docs / steering
- cleanup_csv_contract, inventory-pipeline, context/consultant_context, initiatives,
  review.9.Deep (+ deep_dive latest: deploy gap vs origin/heroku),
  preprocessing_page_review, consult_design_final_review, development.md Procfile spelling,
  .gitignore (frontend/.vite, .pytest_cache), code.9.Push protocol.

Receiving / orders (already in CHANGELOG [2.20.0])
- Earlier commits include v2.20.0 receiving + PO dashboard; see dated section.
