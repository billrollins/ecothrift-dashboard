feat(processing): retail/price lock with editable percent badge

Release: v2.70.0

- Add lock toggle + % badge on Processing row details and shared check-in fields
- Lock scales price when retail changes; % edit updates shelf price (never reverse)
- Persist lock preference in localStorage; row-detail overlay matches check-in feel
- Fix processing-row-patch: keep final_price with shelf_price; denorm no longer clobbers
- Restore start_dashboard / start_website / start_all as distinct staff / public / full starters
