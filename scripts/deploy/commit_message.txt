### 2026-04-06 — review_bump (POS v2.2.9 + docs)

**Release:** Patch **v2.2.9** — POS unscannable (pink tag) manual cart line (already implemented in code this session).

**CHANGELOG:** [2.2.9] section — `POST /api/pos/carts/{id}/add-manual-line/`, terminal Unscannable item dialog, tests `test_cart_manual_line.py`.

**Initiative:** `.ai/initiatives/pos_unscannable_manual_line.md` + row in `.ai/initiatives/_index.md`.

**Bookkeeping:** `.version` → `v2.2.9`, root `package.json` version aligned. `.ai/context.md`, `.ai/extended/pos-system.md`, `README.md` timestamps.

**Protocol:** `.ai/protocols/review_bump.md` Parts A–C (initiative gate satisfied), D (this file), E handoff in session reply.

Suggested conventional commit:

```
feat(pos): unscannable manual line API and terminal (v2.2.9)

- POST .../add-manual-line/ for item-null cart lines
- Terminal dialog + Pink tag chip; tests
- Docs: initiative, CHANGELOG, context, pos-system
```

---
