fix: catalog product check-in creates items again

### Fixed
- product_check_in() now calls processing_row_check_in() after processing_add_item
  so POST /inventory/products/:id/check-in/ creates ItemCheckIn, Items, and label preview
  (regression since v2.32 unmanifested row split)

### Changed
- Workbench navigates to {checkin=<id>} search after successful create check-in
- No success toast when API returns created_count 0

Deploy: backend required (processing_ops.py); frontend recommended for navigation/errors.
