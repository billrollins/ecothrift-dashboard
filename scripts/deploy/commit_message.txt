release(v2.32.0): processing unmanifested lines and queue resume UX

User-facing theme: Processing workspace unmanifested lines, faster queue
resume, and tighter check-in UX.

Added
- Unmanifested add lines (row_kind=added) via POST …/processing-add-item/
- Delete empty added rows via POST …/processing-delete-added-row/
- Multi-chip OR queue filters including unmanifested (segments param)
- Session resume: search + open row detail per PO (sessionStorage)
- Scan search history (10) and Recent row chips (10), client-only
- Refresh page on row detail without browser reload
- Google search shortcuts on product editor, add dialog, and product cards

Changed
- Set/part product links scale check-in price/retail from row bookmark
- Added rows use attach-product-then-check-in (no standalone Check in CTA)
- Simplified Add unmanifested line dialog (title, model, brand)

Tests
- Added-row create/check-in/delete, OR filters, set/part scaling, audit isolation

Initiative: product_item_crud_and_processing (Phase 4–6)
