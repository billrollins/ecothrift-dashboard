fix(inventory): v2.22.1 stop Item Processor row-detail timeouts

- Route `processing_row_detail` through slim `PurchaseOrderViewSet` queryset; drop
  `prefetch_related('manifest_rows')` on PO retrieve (stats annotations unchanged).
- Frontend: remove `ProcessingQueueTable` hover prefetch; row detail query retry and
  window-focus refetch disabled in `useProcessingRowDetail`; remove
  `prefetchProcessingRowDetail`.
- Regression tests in `test_processing_validation_matrix.py`; `.ai` steering aligned
  with `.version`.
