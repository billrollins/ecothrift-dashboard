release(v2.31.0): processing product-linked check-ins

Ship the processing workspace product-linked check-in pass: row/check-in
product links, cleaner prior history, inline batch edits, and updated
processing validation.

## Processing Workspace
- Add migration 0066 for processing row product links.
- Rework prior check-ins into a flat measured table with product editor
  access from ID/Brand/Title/Model/Category cells.
- Allow prior check-in product remap among row-attached products.
- Keep inline Condition / Dispatch / Price edits; remove Status from
  prior check-ins.
- Normalize dispatch labels to canonical capitalization.

## Backend
- Preserve row/check-in product linkage through workspace serializers and
  mutation patches.
- Reject manual processing-patch status=sold; Sold remains POS-controlled.

## Tests
- Add/extend processing split, collapse, identity, validation, queue layout,
  manifest accounting, and prior-history layout tests.

Initiative: product_item_crud_and_processing (processing integration)
