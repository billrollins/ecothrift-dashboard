release(v2.30.0): Inventory Catalog — unified search and in-place CRUD

Ship the Inventory Catalog page (nav Catalog) replacing separate Manage
Products/Items pages. One search bar, three tabs, split detail panels.

## Inventory Catalog
- /inventory/workbench with Products, Check-ins, Items tabs
- Rich search syntax {product=…; checkin=…} and URL state
- Product, item, and check-in edit/create in detail panels
- Legacy manage-products / manage-items redirect with query preserved
- Save+print three-way dialog; ConfirmDialog modals; copyable ID chips

## Backend
- GET /api/inventory/item-check-ins/ catalog API
- Migrations 0063–0065: Item.check_in FK, drop item_ids JSON, indexes
- ItemCheckIn pagination count isolated from Item list cache

## Fixes
- Free tab switching without selection bounce
- Retail/Price decimal input while typing

Initiative: product_item_crud_and_processing (catalog consolidation)
