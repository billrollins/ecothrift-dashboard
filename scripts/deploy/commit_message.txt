feat(inventory): lean preprocessing download CSV + steering (CHANGELOG unreleased)

GET …/download-cleanup-csv/ exports a lean pre-AI CSV (standard row fields +
PurchaseOrder.compute_item_cost → base_cost; ideal_price = 2× unit base cost).
upload-cleanup-csv / apply-cleanup-csv narrow schema unchanged (→ ai_suggested_*).
RowProcessingPanel log copy; test_download_cleanup_csv_staging_lean_schema_and_row_order.

Steering: order_processing_pipeline_rebuild Step 2 export vs apply; context.md +
consultant_context.md; extended/backend.md + inventory-pipeline.md; initiatives _index;
README review.0.Bump protocol row (local commit, no push).

CHANGELOG [Unreleased] — no .version bump per review.0.Bump (release via session.9.Close).
