chore: AI steering cleanup, archive buying/UI initiatives, prune cruft

Documentation and repo hygiene only — product version stays at v2.15.3
(CHANGELOG [Unreleased] holds steering notes; no .version bump in this pass).

Summary
-------

* **`.ai/`** — Refresh `context.md`, `consultant_context.md`, and extended
  domain docs; add `extended/consultant_handoff.md` and
  `protocols/review_bump.md`; drop `consult_retire_charlie.md` /
  `consult_retire_scout.md`; remove `personas/`; align protocols
  (`startup`, `session_checkpoint`, `session_close`, `get_bearing`).
* **Initiatives** — Move `bstock_auction_intelligence.md` and
  `ui_ux_polish.md` to `_archived/_completed/`; clear **Active** table in
  `_index.md`; update `ARCHIVE.md` and cross-links in archived/pending
  initiatives (create_location_label, handoff paths, etc.).
* **Repo root** — Delete `template.env`, `extract-env-vars.bat` (use
  `.env.example` → `.env`).
* **`scripts/deploy/`** — Remove `sync_env_to_heroku.bat` /
  `sync_env_to_heroku.py`, `finish_production_backfill.bat`,
  `run_production_backfill.bat` (obsolete or superseded flows).
* **Workspace** — Remove empty notebook `README.md` stubs under
  `workspace/notebooks/`; drop `workspace/data/.gitkeep` from tracking
  (see staged diff).
* **`CHANGELOG.md`** — Expand `[Unreleased]` (personas, consultant
  handoff, env templates, initiatives index, review_bump audit, workspace
  hygiene).
* **`README.md`**, **`.env.example`**, **`.gitignore`** — Match current
  layout and ignore rules.

Code touch (minor)
------------------

* `apps/buying/management/commands/benchmark_manifest_pull.py`,
  `apps/buying/services/manifest_dev_timelog.py`
* `ecothrift/settings.py`
* `scripts/data/build_sell_through_rates.py`

Pre-commit (before push)
------------------------

* `python manage.py check`
* `cd frontend && npx tsc --noEmit`

---

Replace this file’s body with `---` after you commit, or edit the next
message here before staging.
