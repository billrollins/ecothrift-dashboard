fix(pos): v2.56.2 Field list bottom nav + shared Test preference

Days/Deliveries/Test as a fixed Field bottom button bar (icons,
selected state). Shared Include [TEST] across Desk and Field so phone
QA can see seeded datasets. Open-day shortcuts unchanged.
