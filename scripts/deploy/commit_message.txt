feat: Labor Day 10%, Summer 50%, and Assembly $35 at the register

Labor Day runs itself Mon-Sat of Labor Day week with a header chip cashiers can toggle. Summer is one tap per line and stays 50% when Labor Day is off. Assembly is a $35 line; Assembly and Delivery never take a sale. CartLine keeps list price on unit_price and applies sale_percent into line_total (pos.0027); completing a sale writes the effective price to Item.sold_for and consignment sale_amount. Receipts print the sale price with a Labor Day / Summer suffix.

Also: print server 1.5.1 adds the 1.25x1.25 square label preset; flatten.py imports pymupdf; dev.ps1 stops treating migrate --check stderr as fatal.

Release: v2.88.0
