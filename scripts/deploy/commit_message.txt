release(v2.39.0): floorplan builder, TARS hardening, vendor order dropdown

Release: v2.39.0

### Added
- Floorplan builder — Django `apps/floorplan` (plans + image assets API), SVG editor under Floor Ops, nav entry, JSON/PNG export
- Custom element images — per-location asset library (SVG/PNG/JPEG), assignable per element or placed from Custom palette

### Changed
- TARS restoration hardening — bench drafts, queue patches, parts orders, work_session validation, migration 0078 (remove executing stage)

### Fixed
- Add Order vendor dropdown — show all active vendors instead of hardcoded seven-name whitelist
- TARS bugs — send dialog render loop, requeue reset, cache invalidation, money validation, dashboard done metrics
