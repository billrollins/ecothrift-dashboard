release(v2.18.2): Buying UX — snappier auction list, detail gauge & category mix

## Summary
- Active auctions: optimistic React Query cache (no full-list invalidation on
  thumbs/watch/archive); refetchOnMount false + staleTime on list hooks. Single-
  row archive 2s grace with cancel (useBuyingArchiveGrace). Neighbor page
  prefetch; pagination with results; two-column filters + ABA category-need
  rhythm; desktop row polish.
- Auction detail: max-bid gauge 0→break-even; shared Tgt/Mod/BE tooltips on
  tiles + gauge (AuctionSecondaryCard). Category mix Units + footer total
  (AuctionValuationCard).
- Fix: thumbs cell stopPropagation for non-admin (no accidental navigation).

## Docs
CHANGELOG [2.18.2] · .ai/extended/ux-spec.md · .ai/context.md · .ai/extended/frontend.md
