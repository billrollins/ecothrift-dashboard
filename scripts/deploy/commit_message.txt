docs: name the local shared DB and add parent comm

Release: v2.90.2

Local default database name is local_shared. Production still uses
DATABASE_URL. Steering docs and pull/restore examples match. Parent
master comm protocol lives under .ai/comm and check_comm.md.
