fix(auth): Manager Settings + UserSerializer roles (v2.18.1)

Canonical roles from Django groups; /api/auth/me/ includes roles list.
Sidebar uses rank-based nav; /admin/settings uses ManagerRoute.
Remove redundant DRF source on UserSerializer.roles (fixes login 500).
