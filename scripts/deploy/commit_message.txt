feat: one-window CardX tender and pulse both drawer pins

Release: v2.93.0

POS:
- Card amount is auto-filled (card = total, split = total minus cash).
  No typed card field; split blocks when cash is blank or covers the total.
- CardTenderDialog is one window: TYPE THIS INTO CARDX, YES if asked,
  CARDX DIDN'T ASK / SURCHARGED. Atkinson Hyperlegible. Arrow/1/2/Esc.
- Terminal warns if a cash/split receipt prints but the drawer did not open.

Print server 1.8.0:
- drawer_pin 0 | 1 | both (default both). Longer kick pulse (60/120).
- Kick bytes prepended to the receipt RAW job.
- POST /drawer/control accepts optional pin. Settings > Printing has
  Drawer pin + Open drawer.

Docs: CHANGELOG 2.93.0, pos-system, print-server, cardx_surcharge.
