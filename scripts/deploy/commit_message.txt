feat(inventory): PO Raw Manifest upload on Order detail + hardened upload-manifest

Order detail Raw Manifest card: CSV upload/replace via useUploadManifest / POST …/upload-manifest/
(multipart FormData; axios strips Content-Type so boundary is set). Preprocessing unlocks when
manifest_file exists.

Backend: structured error codes (missing_file, decode_error, empty_csv, storage_error, save_error);
link new S3File before deleting prior blob; process-manifest ManifestRow replace in transaction.atomic().

Docs: CHANGELOG [Unreleased]; .ai context/consultant_context/extended frontend + backend +
inventory-pipeline; initiative inventory_intake_pipeline Session 2; initiatives _index active row.

No .version bump (review_bump — run session_close to release semver).
