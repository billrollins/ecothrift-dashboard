release(v2.34.0): TARS 2 bench live, dashboard metrics, parts orders

Release: v2.34.0

### Restoration
- Live RestorationJob queue API (migrations 0068–0074) and /restoration/queue React Query wiring
- TARS 2 bench workstation (/restoration/tars-2) with persisted work_session, scan-to-bench, timer flows
- Parts drawer Parts + Orders tabs; TarsPartsOrderDialog with order-specific partQtyOverrides
- Debounced work session draft fixes input lag on bench text fields

### Dashboard
- GET /api/pos/dashboard/metrics/ with sales + department rollups (45s cache, DB indexes)
- DashboardSalesGoal + DashboardDepartmentGoal models and editable goal UI
- Client sessionStorage placeholder for sub-second dashboard reload

### HR
- Time & payroll week-hours column, overtime formatting, 16h shift validation

### Inventory / Processing
- Label printed tracking (label_printed_at, mark-labels-printed API)
- Restoration check-in grade scale validation; edit check-in ProductSummaryCard + stat cards
- Status removed as manual check-in input

### Other
- Legacy /restoration/tars mock route retained; TarsMockProvider scoped to that route only
- AI push protocol: mandatory semver bump on every GitHub push
