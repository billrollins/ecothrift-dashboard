fix: POS cart totals stale prefetch; release v2.2.7

- Cart.recalculate() sums CartLine rows from the DB so aggregates never use a
  stale prefetch_related cache after add-item or line edits.
- Add apps/pos/tests/test_cart_totals.py and ecothrift/test_settings.py for
  SQLite-backed test runs.
- Ship [Unreleased] fixes: POS totals (initiative pos_cart_total_stale_prefetch_bug.md),
  Django admin /db-admin routing (django_admin_legacy_navigation).

See CHANGELOG.md [2.2.7].

---

chore(release): v2.2.8 — POS sold SKU, audit, resale copy

- ItemScanHistory outcomes + cart add-resale-copy; CartLine resale_source_* for
  staff/transaction detail; Terminal modal; customer receipts unchanged
  (description only). Migrations: inventory + pos.
- Docs: CHANGELOG, .ai/context.md, initiatives index, pos_sold initiative.
- See CHANGELOG.md [2.2.8]; initiatives pos_sold_item_scan_ux_and_audit_trail.md,
  pos_cart_total_stale_prefetch_bug.md (fixed 2.2.7).

---

docs(protocol): review_bump — commit_message append semantics

- Part D: scripts/deploy/commit_message.txt is a running log since last commit;
  append new work; do not replace prior blocks unless file is --- / empty.
- After commit, reset file to --- (per §16).
- Timestamp on .ai/protocols/review_bump.md.
