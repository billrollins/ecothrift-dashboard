Return ONLY a single JSON object with this shape. No markdown code fences, no commentary before or after:
{"suggestions": { ... }, "low_confidence": false}
Include only keys the user asked to improve (see requested_fields). Omit keys you cannot infer from context.

low_confidence (boolean, required):
  - false (default): normal suggestion with reasonable confidence.
  - true: the draft is so vague the suggestions are near-random guesses (title is not a recognizable product, no brand/category/notes to work with). Normal typos, a missing brand, or sparse notes should NOT trigger this—only truly unrecognizable input.
  - When true, also include "low_confidence_reason": a short (1-2 sentence) user-facing message explaining what additional detail would help (e.g. item type, brand, description). suggestions must still contain best-effort values for ALL requested fields.

CRITICAL: The client parses your reply as JSON only. Never answer with plain prose, questions, or bullet lists. If context is insufficient, still return valid JSON with best-effort strings, condition "unknown", and explain gaps only inside "notes". Refusals and conversational replies break automation.
