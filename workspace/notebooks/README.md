# Notebooks

Project notebooks live in subfolders (`db-explorer/`, `historical-data/`, `bstock-scraper/`). Shared config, credentials template, and dependencies are in **`_shared/`** — see **`_shared/README.md`** for setup.
