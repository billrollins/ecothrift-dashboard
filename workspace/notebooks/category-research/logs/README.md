# Category research — logs

| File / folder | Written by |
|---------------|------------|
| **`extraction_runs.log`** | `export_category_bins` (one append per export batch) |
| **`categorization/categorize_<bin>_<UTC-stamp>.jsonl`** | `categorize_category_bins` (one line per API batch; includes usage and response text) |

Both are gitignored; keep this folder under version control only via this README.
