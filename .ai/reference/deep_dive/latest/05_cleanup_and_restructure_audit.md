# Cleanup And Restructure Audit

## Executive Summary

- **Safe cleanup candidates:** Python **`__pycache__`**, Vitest prebundles under **`frontend/node_modules/.vite/`**, and **`frontend/dist/`** — should remain **uncommitted** (verify `.gitignore`; discard local artifacts before commits).
- **Needs classification:** Large drops under **`.ai/reference/`** (e.g. zip bundles, debug logs) — keep if intentional reference; otherwise move to `workspace/` or delete locally after user confirms.
- **Restructures:** Splitting `apps/inventory/views.py` is **not** recommended without a driving initiative — high churn file; prefer incremental extraction only when touching adjacent features.
- **Confidence:** **High** for generic hygiene; **Low** for deleting any `.ai/reference/*` without user approval.

## Generated / Disposable Candidates

| Path | Type | Evidence | Safe to remove? | Approval needed | Notes |
|------|------|----------|-----------------|-----------------|-------|
| `apps/**/__pycache__/` | bytecode cache | Common Python artifact | yes (local) | no | Never commit |
| `frontend/dist/` | build output | Vite production bundle | yes (local) | no | Regenerated by `npm run build` |
| `frontend/node_modules/.vite/` | Vite cache | Dev dependency cache | yes (local) | no | Regenerated |
| `.pytest_cache/` | test cache | pytest | yes (local) | no | `.gitignore` already mentions per `CHANGELOG` |

## Duplicate / Stale Reference Candidates

| Path / reference | Duplicate of / replaced by | Evidence | Recommendation | Priority |
|------------------|---------------------------|----------|----------------|----------|
| `.ai/initiatives/_protocols/README.md` | Canonical content in `_archived/_protocols/` | Stub README states pointer pattern | No merge needed unless removing stub for simplicity | **P3** optional UX |
| Multiple Final Review specs (`fix_this.md`, directive, plan, pass plan) | Intentional layering (directive > plan) | `CHANGELOG [Unreleased]` | Keep; do not delete without user | — |

## Documentation Restructure Candidates

| Area | Current problem | Proposed structure | Why | Risk |
|------|-----------------|-------------------|-----|------|
| Deep dive outputs | Multiple timestamps under `_runs/` | Keep **`latest/`** + dated `_runs/` only | Matches protocol | Low |

## Code Restructure Candidates

| Area | Current problem | Proposed structure | Evidence | Risk | Suggested timing |
|------|-----------------|-------------------|----------|------|------------------|
| `PurchaseOrderViewSet` size | Many `@action` methods in one class | Extract serializers/helpers already partly done (`processing_workspace.py`, `cleanup_csv_validate.py`) | `apps/inventory/views.py` grep shows dense actions | High if rushed | **Only if touched** |

## Removal Safety Notes

- **Secrets / env:** Never commit `.env`; `.env.example` is the public template.
- **Production exports:** No DB dumps observed in this audit pass — if added under `workspace/`, keep gitignored.
- **User reference assets:** `.ai/reference/` may contain consultant artifacts — classify before deletion.
- **Generated JS bundles:** `frontend/dist/assets/*.js` must not land in commits for routine dev.

## Notes For `PLAN.md`

- **`DEL-001`**: Working-tree hygiene sweep (`__pycache__`, `dist`) — source: Generated table.
- **`STRUCT-001`**: Code — defer splitting **`PurchaseOrderViewSet`** unless initiative-owned — source: Code Restructure table (`INIT-001` in `PLAN.md` covers optional `_index.md` protocol link).
