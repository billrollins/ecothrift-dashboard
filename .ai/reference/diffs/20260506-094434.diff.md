# Diff summary — 2026-05-06 09:44 (local timezone)

## Metadata

- **Repo:** `ecothrift-dashboard` (local path: `E:\Cursor Projects\ecothrift-dashboard`)
- **Local branch:** `main` @ `960597ced77da129f6c2cfa6d1b2e847bd43ec98`
- **Compared to:** `origin/main` @ `1afb8314a2bb3ee9327beed5f6b29dc4decee84c`; `heroku/main` @ `960597ced77da129f6c2cfa6d1b2e847bd43ec98`
- **Working tree:** **dirty** — modified `.ai/protocols/code.0.Startup.md`; untracked `.ai/protocols/review.1.Diff.md`. `git diff R...HEAD` does **not** include these uncommitted paths.

## Versions (.version)

| Ref | Value |
|-----|-------|
| working tree | v2.22.1 |
| origin/main | v2.22.1 |
| heroku/main | v2.22.1 |

## origin/main vs HEAD

- **Behind / ahead:** `1 / 1` (`git rev-list --left-right --count origin/main...HEAD`)
- **Diverged:** **yes**
- **Files differ:** **0** total (0 added, 0 removed, 0 modified, 0 renamed)
- **`git diff --shortstat`:** _(none — empty tree diff)_

**Interpretation:** **Same tree as `HEAD`, different commit history:** local `HEAD` and `origin/main` each tip a distinct commit (`960597c` vs `1afb831`) while the snapshots match (no changed files vs `HEAD`).

### name-status

```
(no output — identical trees)
```

### `git log --oneline --left-right HEAD...origin/main`

```
< 960597c fix(inventory): v2.22.1 stop Item Processor row-detail timeouts
> 1afb831 fix(inventory): v2.22.1 Item Processor row-detail timeouts
```

## heroku/main vs HEAD

- **Behind / ahead:** `0 / 0` (`git rev-list --left-right --count heroku/main...HEAD`)
- **Diverged:** **no**
- **Files differ:** **0** total (0 added, 0 removed, 0 modified, 0 renamed)
- **`git diff --shortstat`:** _(none — empty tree diff)_

### name-status

```
(no output — identical trees)
```

## Notes

- **`git fetch`** against `origin` and `heroku` was run immediately before these reads (2026-05-06).
- **`heroku/master`** also exists on this remote (`git branch -r`); this report uses **`heroku/main`** per protocol defaults. **`heroku/main`** **equals** **`HEAD`** for this workspace.
- **GitHub** is **out of sync** with local/Heroku at the commit-graph level (**1 behind / 1 ahead**) but **no file-level delta** vs current `HEAD` — typical pattern after **`git commit --amend`**, rewrote message, or replaced a cherry-picked tip with equivalent tree.
- Uncommitted edits under `.ai/protocols/` are **not** reflected in comparisons above until committed.
